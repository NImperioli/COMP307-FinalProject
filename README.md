# Getting Started Guide

To start Mongodb type mongod in terminal from the backend directory.

To start the Node.js server simply type npm start

To access the db type mongosh > show dbs > use db_name > show collections > db.collectionname.find()